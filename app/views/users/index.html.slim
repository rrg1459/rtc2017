- provide(:title, 'Usuarios')

.row
  .col-md-8
    .timeline
      h2.title Todos los Usuarios
      ul.tweets
        - @users.each do |user|
          li.tweet.clearfix
            = gravatar_for user, size: 52
            = link_to user.name, user
